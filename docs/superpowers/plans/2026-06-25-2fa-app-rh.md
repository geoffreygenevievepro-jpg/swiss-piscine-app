# 2FA App RH (TOTP + Email + appareil de confiance) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter une 2FA (TOTP via appli authenticator, ou code par Email, au choix de l'employé) avec appareil de confiance 30 jours, à l'app RH FastAPI + PWA, sans alourdir le quotidien.

**Architecture:** Après login+PIN (et le gate Supabase existant), `auth.py` n'émet plus les tokens directement : si l'appareil n'est pas de confiance, il renvoie un **token pré-auth** court et exige un 2e facteur via les endpoints `/2fa/*`. Logique 2FA isolée dans `app/twofa.py` (TOTP `pyotp`, chiffrement `cryptography` Fernet, codes email, jetons d'appareil), état en SQLite (colonnes `employees` + tables `email_otps`, `trusted_devices`). Frontend : un écran `twofa.js` (setup/verify) intercalé dans `login.js`.

**Tech Stack:** Python 3.12, FastAPI, SQLite, PyJWT, `pyotp`, `cryptography` (Fernet), pytest ; frontend PWA vanilla JS.

## Global Constraints
- Méthodes : **TOTP** (appli authenticator) **ou Email** (code 6 chiffres), au choix de l'employé. Pas de SMS.
- **Email masqué si `work_email` Odoo vide** (`can_email=false`) → seul TOTP.
- Email = **code 6 chiffres**, **expiration 10 min**, **max 5 essais**, **anti-renvoi 1/60 s et 5/heure**. Envoi via Odoo `mail.mail` (best-effort).
- Secret TOTP **chiffré au repos** (Fernet, clé dérivée de `settings.resolved_jwt_secret()`).
- Appareil de confiance : token aléatoire 32 octets, **stocké haché**, cookie **HttpOnly+Secure+SameSite=Lax**, **30 jours**, révocable.
- Token pré-auth : JWT `type="pre2fa"`, ~5 min, n'accède **qu'aux endpoints `/2fa/*`**.
- Le **gate Supabase** (`supabase_access.access_decision`) reste **avant** la 2FA dans `login()`.
- Reset en cas de perte = **admin only** (`role=='admin'`), endpoint `POST /admin/employees/{id}/reset-2fa`.
- Tests : `pytest` lancé via `backend/.venv/bin/python -m pytest backend/tests/`.
- Tout le code SQLite via `db.get_conn()` ; horodatages via `db._utcnow_iso()`.

---

### Task 1 : Dépendances + migration SQLite

**Files:**
- Modify: `backend/requirements.txt`
- Modify: `backend/app/db.py` (SCHEMA + `init_db` migration + helpers)
- Test: `backend/tests/test_twofa_db.py`

**Interfaces:**
- Produces (db.py) :
  - colonnes `employees` : `twofa_method TEXT`, `twofa_secret_encrypted TEXT`, `twofa_enabled INTEGER DEFAULT 0`
  - `set_twofa(emp_id:int, method:str, secret_encrypted:str|None) -> None` (active : `twofa_enabled=1`)
  - `reset_twofa(emp_id:int) -> None` (efface method/secret, `twofa_enabled=0`)
  - `add_trusted_device(employee_id:int, token_hash:str, expires_at_iso:str, user_agent:str) -> None`
  - `is_device_trusted(employee_id:int, token_hash:str, now_iso:str) -> bool`
  - `revoke_trusted_devices(employee_id:int) -> None`
  - `create_email_otp(employee_id:int, code_hash:str, expires_at_iso:str) -> None` (remplace tout OTP existant de l'employé)
  - `get_email_otp(employee_id:int) -> sqlite3.Row | None`
  - `bump_email_otp_attempts(employee_id:int) -> None`
  - `delete_email_otp(employee_id:int) -> None`
  - `count_recent_email_otps(employee_id:int, since_iso:str) -> int`

- [ ] **Step 1 : Écrire le test**

```python
# backend/tests/test_twofa_db.py
from datetime import datetime, timedelta, timezone
import app.db as db

def _iso(dt): return dt.isoformat()

def test_twofa_columns_and_set_reset(tmp_path, monkeypatch):
    monkeypatch.setattr(db.settings, "db_path", str(tmp_path / "t.db"))
    db.init_db()
    db.upsert_employee(1, "alex", "Alex", "manager", "h")
    emp = db.get_employee_by_login("alex")
    assert emp["twofa_enabled"] == 0
    db.set_twofa(emp["id"], "totp", "ENC")
    emp = db.get_employee_by_id(emp["id"])
    assert emp["twofa_enabled"] == 1 and emp["twofa_method"] == "totp" and emp["twofa_secret_encrypted"] == "ENC"
    db.reset_twofa(emp["id"])
    emp = db.get_employee_by_id(emp["id"])
    assert emp["twofa_enabled"] == 0 and emp["twofa_method"] is None and emp["twofa_secret_encrypted"] is None

def test_trusted_device(tmp_path, monkeypatch):
    monkeypatch.setattr(db.settings, "db_path", str(tmp_path / "t.db"))
    db.init_db(); db.upsert_employee(1, "alex", "Alex", "tech", "h")
    eid = db.get_employee_by_login("alex")["id"]
    now = datetime.now(timezone.utc)
    db.add_trusted_device(eid, "HASH", _iso(now + timedelta(days=30)), "UA")
    assert db.is_device_trusted(eid, "HASH", _iso(now)) is True
    assert db.is_device_trusted(eid, "OTHER", _iso(now)) is False
    # expiré
    assert db.is_device_trusted(eid, "HASH", _iso(now + timedelta(days=31))) is False
    db.revoke_trusted_devices(eid)
    assert db.is_device_trusted(eid, "HASH", _iso(now)) is False

def test_email_otp(tmp_path, monkeypatch):
    monkeypatch.setattr(db.settings, "db_path", str(tmp_path / "t.db"))
    db.init_db(); db.upsert_employee(1, "alex", "Alex", "tech", "h")
    eid = db.get_employee_by_login("alex")["id"]
    now = datetime.now(timezone.utc)
    db.create_email_otp(eid, "CH", _iso(now + timedelta(minutes=10)))
    row = db.get_email_otp(eid)
    assert row["code_hash"] == "CH" and row["attempts"] == 0
    db.bump_email_otp_attempts(eid)
    assert db.get_email_otp(eid)["attempts"] == 1
    assert db.count_recent_email_otps(eid, _iso(now - timedelta(hours=1))) == 1
    db.delete_email_otp(eid)
    assert db.get_email_otp(eid) is None
```

- [ ] **Step 2 : Lancer le test (échoue)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_twofa_db.py -q`
Expected: FAIL (colonnes/fonctions absentes).

- [ ] **Step 3 : Migration + helpers**

Dans `backend/requirements.txt`, ajouter :
```
pyotp>=2.9
cryptography>=42.0
```

Dans `backend/app/db.py`, ajouter aux `SCHEMA` (avant la fermeture `"""`), les 2 nouvelles tables :
```sql
CREATE TABLE IF NOT EXISTS trusted_devices (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    employee_id INTEGER NOT NULL,
    token_hash  TEXT NOT NULL,
    expires_at  TEXT NOT NULL,
    user_agent  TEXT,
    created_at  TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_trusted_emp ON trusted_devices(employee_id);

CREATE TABLE IF NOT EXISTS email_otps (
    employee_id INTEGER PRIMARY KEY,
    code_hash   TEXT NOT NULL,
    expires_at  TEXT NOT NULL,
    attempts    INTEGER NOT NULL DEFAULT 0,
    created_at  TEXT NOT NULL
);
```

Remplacer `init_db` pour ajouter la migration des colonnes (idempotente) :
```python
def init_db() -> None:
    Path(settings.db_path).parent.mkdir(parents=True, exist_ok=True)
    with get_conn() as conn:
        conn.executescript(SCHEMA)
        # Colonnes 2FA ajoutées aux installations existantes (ALTER idempotent).
        for ddl in (
            "ALTER TABLE employees ADD COLUMN twofa_method TEXT",
            "ALTER TABLE employees ADD COLUMN twofa_secret_encrypted TEXT",
            "ALTER TABLE employees ADD COLUMN twofa_enabled INTEGER NOT NULL DEFAULT 0",
        ):
            try:
                conn.execute(ddl)
            except sqlite3.OperationalError:
                pass  # colonne déjà présente
```

Ajouter les helpers (section « Employés » et nouvelles sections) :
```python
def set_twofa(emp_id: int, method: str, secret_encrypted: str | None) -> None:
    with get_conn() as conn:
        conn.execute(
            "UPDATE employees SET twofa_method = ?, twofa_secret_encrypted = ?, twofa_enabled = 1 WHERE id = ?",
            (method, secret_encrypted, emp_id))

def reset_twofa(emp_id: int) -> None:
    with get_conn() as conn:
        conn.execute(
            "UPDATE employees SET twofa_method = NULL, twofa_secret_encrypted = NULL, twofa_enabled = 0 WHERE id = ?",
            (emp_id,))

# --- Appareils de confiance ---
def add_trusted_device(employee_id: int, token_hash: str, expires_at_iso: str, user_agent: str) -> None:
    with get_conn() as conn:
        conn.execute(
            "INSERT INTO trusted_devices (employee_id, token_hash, expires_at, user_agent, created_at) "
            "VALUES (?, ?, ?, ?, ?)", (employee_id, token_hash, expires_at_iso, user_agent, _utcnow_iso()))

def is_device_trusted(employee_id: int, token_hash: str, now_iso: str) -> bool:
    with get_conn() as conn:
        row = conn.execute(
            "SELECT 1 FROM trusted_devices WHERE employee_id = ? AND token_hash = ? AND expires_at > ? LIMIT 1",
            (employee_id, token_hash, now_iso)).fetchone()
        return row is not None

def revoke_trusted_devices(employee_id: int) -> None:
    with get_conn() as conn:
        conn.execute("DELETE FROM trusted_devices WHERE employee_id = ?", (employee_id,))

# --- Codes OTP email ---
def create_email_otp(employee_id: int, code_hash: str, expires_at_iso: str) -> None:
    with get_conn() as conn:
        conn.execute(
            "INSERT INTO email_otps (employee_id, code_hash, expires_at, attempts, created_at) "
            "VALUES (?, ?, ?, 0, ?) "
            "ON CONFLICT(employee_id) DO UPDATE SET code_hash=excluded.code_hash, "
            "expires_at=excluded.expires_at, attempts=0, created_at=excluded.created_at",
            (employee_id, code_hash, expires_at_iso, _utcnow_iso()))

def get_email_otp(employee_id: int) -> sqlite3.Row | None:
    with get_conn() as conn:
        return conn.execute("SELECT * FROM email_otps WHERE employee_id = ?", (employee_id,)).fetchone()

def bump_email_otp_attempts(employee_id: int) -> None:
    with get_conn() as conn:
        conn.execute("UPDATE email_otps SET attempts = attempts + 1 WHERE employee_id = ?", (employee_id,))

def delete_email_otp(employee_id: int) -> None:
    with get_conn() as conn:
        conn.execute("DELETE FROM email_otps WHERE employee_id = ?", (employee_id,))

def count_recent_email_otps(employee_id: int, since_iso: str) -> int:
    with get_conn() as conn:
        return conn.execute(
            "SELECT COUNT(*) FROM email_otps WHERE employee_id = ? AND created_at >= ?",
            (employee_id, since_iso)).fetchone()[0]
```

> Note : `count_recent_email_otps` sur une table à 1 ligne/employé compte le dernier envoi ; l'anti-renvoi « 5/heure » réel s'appuie surtout sur l'anti-renvoi « 1/60 s » côté router (Task 5) — gardé simple pour V1.

- [ ] **Step 4 : Installer les deps + lancer le test (passe)**

Run: `backend/.venv/bin/python -m pip install -q pyotp cryptography && backend/.venv/bin/python -m pytest backend/tests/test_twofa_db.py -q`
Expected: PASS (3 tests).

- [ ] **Step 5 : Commit**

```bash
git add backend/requirements.txt backend/app/db.py backend/tests/test_twofa_db.py
git commit -m "feat(2fa): migration SQLite (colonnes 2FA + tables trusted_devices/email_otps) + deps"
```

---

### Task 2 : Module `twofa.py` (TOTP, chiffrement, codes, jetons) — TDD

**Files:**
- Create: `backend/app/twofa.py`
- Test: `backend/tests/test_twofa.py`

**Interfaces:**
- Consumes: `settings.resolved_jwt_secret()` (config), `pyotp`, `cryptography.fernet`.
- Produces:
  - `new_secret() -> str` ; `provisioning_uri(secret:str, login:str) -> str` ; `verify_totp(secret:str, code:str) -> bool`
  - `encrypt(s:str) -> str` ; `decrypt(s:str) -> str`
  - `generate_email_code() -> str` (6 chiffres) ; `hash_code(code:str) -> str`
  - `new_device_token() -> str` ; `hash_token(token:str) -> str`

- [ ] **Step 1 : Écrire le test**

```python
# backend/tests/test_twofa.py
import pyotp
import app.twofa as twofa

def test_totp_roundtrip():
    s = twofa.new_secret()
    code = pyotp.TOTP(s).now()
    assert twofa.verify_totp(s, code) is True
    assert twofa.verify_totp(s, "000000") is False

def test_provisioning_uri():
    uri = twofa.provisioning_uri("ABC", "alex")
    assert uri.startswith("otpauth://totp/") and "secret=ABC" in uri and "issuer=" in uri

def test_encrypt_roundtrip():
    enc = twofa.encrypt("topsecret")
    assert enc != "topsecret"
    assert twofa.decrypt(enc) == "topsecret"

def test_email_code_and_hash():
    code = twofa.generate_email_code()
    assert len(code) == 6 and code.isdigit()
    h = twofa.hash_code(code)
    assert h == twofa.hash_code(code) and h != twofa.hash_code("000000")

def test_device_token():
    t = twofa.new_device_token()
    assert len(t) >= 32
    assert twofa.hash_token(t) == twofa.hash_token(t)
    assert twofa.hash_token(t) != twofa.hash_token("x")
```

- [ ] **Step 2 : Lancer (échoue)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_twofa.py -q`
Expected: FAIL (module absent).

- [ ] **Step 3 : Implémenter `twofa.py`**

```python
# backend/app/twofa.py
"""Logique 2FA : TOTP, chiffrement du secret, codes email, jetons d'appareil."""
import base64
import hashlib
import hmac
import secrets

import pyotp
from cryptography.fernet import Fernet

from .config import settings

ISSUER = "Swiss Piscine"


def _fernet() -> Fernet:
    # Clé Fernet (32 octets url-safe b64) dérivée du secret applicatif.
    key = hashlib.sha256(settings.resolved_jwt_secret().encode("utf-8")).digest()
    return Fernet(base64.urlsafe_b64encode(key))


# --- TOTP ---
def new_secret() -> str:
    return pyotp.random_base32()

def provisioning_uri(secret: str, login: str) -> str:
    return pyotp.TOTP(secret).provisioning_uri(name=login, issuer_name=ISSUER)

def verify_totp(secret: str, code: str) -> bool:
    try:
        return pyotp.TOTP(secret).verify(code, valid_window=1)
    except Exception:
        return False


# --- Chiffrement du secret ---
def encrypt(s: str) -> str:
    return _fernet().encrypt(s.encode("utf-8")).decode("utf-8")

def decrypt(s: str) -> str:
    return _fernet().decrypt(s.encode("utf-8")).decode("utf-8")


# --- Codes email ---
def generate_email_code() -> str:
    return f"{secrets.randbelow(1_000_000):06d}"

def hash_code(code: str) -> str:
    return hashlib.sha256((settings.resolved_jwt_secret() + code).encode("utf-8")).hexdigest()


# --- Jetons d'appareil de confiance ---
def new_device_token() -> str:
    return secrets.token_urlsafe(32)

def hash_token(token: str) -> str:
    return hashlib.sha256((settings.resolved_jwt_secret() + token).encode("utf-8")).hexdigest()
```

- [ ] **Step 4 : Lancer (passe)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_twofa.py -q`
Expected: PASS (5 tests).

- [ ] **Step 5 : Commit**

```bash
git add backend/app/twofa.py backend/tests/test_twofa.py
git commit -m "feat(2fa): module twofa (TOTP, Fernet, codes email, jetons appareil)"
```

---

### Task 3 : Token pré-auth (security.py + deps.py) — TDD

**Files:**
- Modify: `backend/app/security.py` (ajout `create_pre2fa_token` + constante TTL)
- Modify: `backend/app/deps.py` (ajout `get_pre2fa_employee`)
- Test: `backend/tests/test_pre2fa.py`

**Interfaces:**
- Consumes: `decode_token` (security), `db.get_employee_by_id`.
- Produces:
  - `security.create_pre2fa_token(employee_id:int) -> str` (JWT `type="pre2fa"`, exp 5 min).
  - `deps.get_pre2fa_employee(creds) -> sqlite3.Row` (n'accepte que `type=="pre2fa"`, sinon 401).

- [ ] **Step 1 : Écrire le test**

```python
# backend/tests/test_pre2fa.py
import pytest
from fastapi import HTTPException
from fastapi.security import HTTPAuthorizationCredentials
import app.db as db
from app.security import create_pre2fa_token, create_access_token
from app.deps import get_pre2fa_employee

def _creds(tok): return HTTPAuthorizationCredentials(scheme="Bearer", credentials=tok)

def test_pre2fa_accepts_only_pre2fa(tmp_path, monkeypatch):
    monkeypatch.setattr(db.settings, "db_path", str(tmp_path / "t.db"))
    db.init_db(); db.upsert_employee(1, "alex", "Alex", "tech", "h")
    eid = db.get_employee_by_login("alex")["id"]
    emp = get_pre2fa_employee(_creds(create_pre2fa_token(eid)))
    assert emp["id"] == eid
    # un token d'accès normal est refusé sur cette dépendance
    with pytest.raises(HTTPException):
        get_pre2fa_employee(_creds(create_access_token(eid, "alex", "tech")))
```

- [ ] **Step 2 : Lancer (échoue)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_pre2fa.py -q`
Expected: FAIL.

- [ ] **Step 3 : Implémenter**

Dans `backend/app/security.py`, après `create_access_token` :
```python
PRE2FA_TTL_MIN = 5

def create_pre2fa_token(employee_id: int) -> str:
    payload = {
        "sub": str(employee_id),
        "type": "pre2fa",
        "iat": _now(),
        "exp": _now() + timedelta(minutes=PRE2FA_TTL_MIN),
    }
    return jwt.encode(payload, settings.resolved_jwt_secret(), algorithm=settings.jwt_algorithm)
```

Dans `backend/app/deps.py`, ajouter :
```python
def get_pre2fa_employee(
    creds: HTTPAuthorizationCredentials | None = Depends(bearer_scheme),
):
    """Valide un token pré-2FA et renvoie la ligne employee. Refuse tout autre type."""
    if creds is None:
        raise HTTPException(status.HTTP_401_UNAUTHORIZED, "Token manquant")
    try:
        payload = decode_token(creds.credentials)
    except jwt.PyJWTError:
        raise HTTPException(status.HTTP_401_UNAUTHORIZED, "Token invalide")
    if payload.get("type") != "pre2fa":
        raise HTTPException(status.HTTP_401_UNAUTHORIZED, "Token pré-2FA requis")
    emp = db.get_employee_by_id(int(payload["sub"]))
    if emp is None:
        raise HTTPException(status.HTTP_401_UNAUTHORIZED, "Compte introuvable")
    return emp
```

- [ ] **Step 4 : Lancer (passe)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_pre2fa.py -q`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add backend/app/security.py backend/app/deps.py backend/tests/test_pre2fa.py
git commit -m "feat(2fa): token pre-auth + dependance get_pre2fa_employee"
```

---

### Task 4 : Envoi d'email via Odoo (`send_email`)

**Files:**
- Modify: `backend/app/odoo.py` (helper `send_email`)
- Test: `backend/tests/test_send_email.py`

**Interfaces:**
- Consumes: client Odoo r/w (`get_write_client()` / pattern existant `rw.execute_kw`).
- Produces: `send_email(to: str, subject: str, body_html: str) -> bool` (best-effort, jamais d'exception).

- [ ] **Step 1 : Écrire le test (mock du client Odoo)**

```python
# backend/tests/test_send_email.py
from unittest.mock import MagicMock
import app.odoo as odoo

def test_send_email_creates_and_sends(monkeypatch):
    rw = MagicMock()
    rw.execute_kw.return_value = 42  # id du mail.mail créé
    monkeypatch.setattr(odoo, "get_write_client", lambda: rw)
    ok = odoo.send_email("a@b.ch", "Sujet", "<p>hi</p>")
    assert ok is True
    models = [c.args[0] for c in rw.execute_kw.call_args_list]
    assert "mail.mail" in models

def test_send_email_best_effort(monkeypatch):
    rw = MagicMock()
    rw.execute_kw.side_effect = RuntimeError("odoo down")
    monkeypatch.setattr(odoo, "get_write_client", lambda: rw)
    assert odoo.send_email("a@b.ch", "S", "B") is False
```

- [ ] **Step 2 : Lancer (échoue)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_send_email.py -q`
Expected: FAIL.

- [ ] **Step 3 : Implémenter `send_email` dans `odoo.py`**

Vérifier le nom exact du client r/w existant (`get_write_client` d'après le code) ; ajouter à la fin de `odoo.py` :
```python
def send_email(to: str, subject: str, body_html: str) -> bool:
    """Envoie un email transactionnel via Odoo (mail.mail). Best-effort."""
    try:
        rw = get_write_client()
        mail_id = rw.execute_kw("mail.mail", "create", [{
            "subject": subject,
            "body_html": body_html,
            "email_to": to,
            "auto_delete": True,
        }])
        rw.execute_kw("mail.mail", "send", [[mail_id]])
        return True
    except Exception:
        return False
```

- [ ] **Step 4 : Lancer (passe)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_send_email.py -q`
Expected: PASS (2 tests).

- [ ] **Step 5 : Commit**

```bash
git add backend/app/odoo.py backend/tests/test_send_email.py
git commit -m "feat(2fa): helper send_email via Odoo (best-effort)"
```

---

### Task 5 : Router `/2fa/*` (setup TOTP/Email, verify, resend, status)

**Files:**
- Create: `backend/app/routers/twofa.py`
- Modify: `backend/app/main.py` (enregistrer le router)
- Test: `backend/tests/test_twofa_router.py`

**Interfaces:**
- Consumes: `deps.get_pre2fa_employee`, `db.*` (Task 1), `twofa.*` (Task 2), `odoo.send_email` (Task 4), `odoo.get_employee` (work_email), `security.create_access_token/create_refresh_token`, `db.store_refresh_token`.
- Produces (endpoints, tous gardés `get_pre2fa_employee`) :
  - `GET /2fa/status` → `{ enabled: bool, method: str|None, can_email: bool }`
  - `POST /2fa/setup/totp/start` → `{ secret, otpauth_uri }` (stocke le secret chiffré candidat en session via un champ temporaire — voir note)
  - `POST /2fa/setup/totp/confirm` `{ code }` → vérifie, `set_twofa(method='totp')`
  - `POST /2fa/setup/email/start` → envoie un code (si `can_email`), sinon 400
  - `POST /2fa/setup/email/confirm` `{ code }` → vérifie, `set_twofa(method='email')`
  - `POST /2fa/verify` `{ code, trust_device? }` → vérifie selon la méthode, renvoie `issue()` (+ cookie appareil)
  - `POST /2fa/email/resend` → renvoie un nouveau code (anti-renvoi)
- Helper interne `issue(emp, response, trust_device)` : émet access+refresh (réutilise `auth._issue_tokens` logique) et pose le cookie appareil de confiance si demandé.

**Note d'implémentation (secret candidat TOTP) :** entre `setup/totp/start` et `confirm`, stocker le secret chiffré candidat dans `employees.twofa_secret_encrypted` avec `twofa_enabled=0` (via une fonction `db.stage_twofa_secret(emp_id, enc)` analogue à `set_twofa` mais sans activer). `confirm` lit ce secret, vérifie le code, puis `set_twofa('totp', enc)`. Ajouter cette fonction dans Task 1 si absente, ou ici. Pour l'email, le code est géré par `email_otps` (Task 1).

- [ ] **Step 1 : Écrire le test (deps & odoo mockés)**

```python
# backend/tests/test_twofa_router.py
from fastapi.testclient import TestClient
import pyotp
import app.db as db
import app.odoo as odoo
import app.twofa as twofa
from app.main import app
from app.deps import get_pre2fa_employee

client = TestClient(app)

def _setup(tmp_path, monkeypatch, work_email="a@b.ch"):
    monkeypatch.setattr(db.settings, "db_path", str(tmp_path / "t.db"))
    db.init_db(); db.upsert_employee(1, "alex", "Alex", "tech", "h")
    emp = db.get_employee_by_login("alex")
    app.dependency_overrides[get_pre2fa_employee] = lambda: emp
    monkeypatch.setattr(odoo, "get_employee", lambda hr: {"work_email": work_email})
    monkeypatch.setattr(odoo, "send_email", lambda *a, **k: True)
    return emp

def teardown_function():
    app.dependency_overrides.clear()

def test_status_can_email(tmp_path, monkeypatch):
    _setup(tmp_path, monkeypatch, work_email="a@b.ch")
    r = client.get("/2fa/status")
    assert r.status_code == 200 and r.json()["can_email"] is True and r.json()["enabled"] is False

def test_totp_setup_and_confirm(tmp_path, monkeypatch):
    emp = _setup(tmp_path, monkeypatch)
    start = client.post("/2fa/setup/totp/start").json()
    secret = start["secret"]
    code = pyotp.TOTP(secret).now()
    r = client.post("/2fa/setup/totp/confirm", json={"code": code})
    assert r.status_code == 200
    assert db.get_employee_by_id(emp["id"])["twofa_enabled"] == 1

def test_email_setup_requires_email(tmp_path, monkeypatch):
    _setup(tmp_path, monkeypatch, work_email="")  # pas d'email
    r = client.post("/2fa/setup/email/start")
    assert r.status_code == 400
```

- [ ] **Step 2 : Lancer (échoue)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_twofa_router.py -q`
Expected: FAIL.

- [ ] **Step 3 : Implémenter le router**

Créer `backend/app/routers/twofa.py` (≈ ci-dessous ; ajouter `db.stage_twofa_secret` dans db.py si non créé en Task 1) :
```python
from datetime import datetime, timedelta, timezone

from fastapi import APIRouter, Depends, HTTPException, Response, status
from pydantic import BaseModel, Field

from .. import db, odoo, twofa
from ..config import settings
from ..deps import get_pre2fa_employee
from ..security import create_access_token, create_refresh_token

router = APIRouter(prefix="/2fa", tags=["2fa"])
TRUST_DAYS = 30
COOKIE = "sp_trust"

def _now(): return datetime.now(timezone.utc)

class CodeBody(BaseModel):
    code: str = Field(..., min_length=4, max_length=8)
    trust_device: bool = False

def _can_email(emp) -> bool:
    try:
        return bool((odoo.get_employee(emp["hr_employee_id"]) or {}).get("work_email"))
    except Exception:
        return False

def _issue(emp, response: Response, trust_device: bool):
    access = create_access_token(emp["id"], emp["login"], emp["role"])
    refresh, jti, exp = create_refresh_token(emp["id"])
    db.store_refresh_token(jti, emp["id"], exp.isoformat())
    if trust_device:
        token = twofa.new_device_token()
        db.add_trusted_device(emp["id"], twofa.hash_token(token),
                              (_now() + timedelta(days=TRUST_DAYS)).isoformat(), "")
        response.set_cookie(COOKIE, token, max_age=TRUST_DAYS * 86400,
                            httponly=True, secure=True, samesite="lax")
    return {"access_token": access, "refresh_token": refresh,
            "expires_in": settings.access_token_ttl_min * 60}

@router.get("/status")
def status_(emp=Depends(get_pre2fa_employee)):
    return {"enabled": bool(emp["twofa_enabled"]), "method": emp["twofa_method"], "can_email": _can_email(emp)}

@router.post("/setup/totp/start")
def totp_start(emp=Depends(get_pre2fa_employee)):
    secret = twofa.new_secret()
    db.stage_twofa_secret(emp["id"], twofa.encrypt(secret))
    return {"secret": secret, "otpauth_uri": twofa.provisioning_uri(secret, emp["login"])}

@router.post("/setup/totp/confirm")
def totp_confirm(body: CodeBody, response: Response, emp=Depends(get_pre2fa_employee)):
    enc = emp["twofa_secret_encrypted"]
    if not enc or not twofa.verify_totp(twofa.decrypt(enc), body.code):
        raise HTTPException(400, "Code invalide")
    db.set_twofa(emp["id"], "totp", enc)
    return _issue(emp, response, body.trust_device)

@router.post("/setup/email/start")
def email_start(emp=Depends(get_pre2fa_employee)):
    info = odoo.get_employee(emp["hr_employee_id"]) or {}
    to = info.get("work_email")
    if not to:
        raise HTTPException(400, "Aucune adresse email")
    code = twofa.generate_email_code()
    db.create_email_otp(emp["id"], twofa.hash_code(code), (_now() + timedelta(minutes=10)).isoformat())
    odoo.send_email(to, "Votre code de connexion", f"<p>Votre code : <b>{code}</b> (valable 10 min)</p>")
    return {"sent": True}

@router.post("/setup/email/confirm")
def email_confirm(body: CodeBody, response: Response, emp=Depends(get_pre2fa_employee)):
    if not _verify_email(emp["id"], body.code):
        raise HTTPException(400, "Code invalide ou expiré")
    db.set_twofa(emp["id"], "email", None)
    return _issue(emp, response, body.trust_device)

@router.post("/verify")
def verify(body: CodeBody, response: Response, emp=Depends(get_pre2fa_employee)):
    if emp["twofa_method"] == "totp":
        enc = emp["twofa_secret_encrypted"]
        ok = bool(enc) and twofa.verify_totp(twofa.decrypt(enc), body.code)
    else:
        ok = _verify_email(emp["id"], body.code)
    if not ok:
        raise HTTPException(400, "Code invalide ou expiré")
    return _issue(emp, response, body.trust_device)

@router.post("/email/resend")
def resend(emp=Depends(get_pre2fa_employee)):
    return email_start(emp)

def _verify_email(emp_id: int, code: str) -> bool:
    row = db.get_email_otp(emp_id)
    if not row:
        return False
    if datetime.fromisoformat(row["expires_at"]) < _now() or row["attempts"] >= 5:
        return False
    db.bump_email_otp_attempts(emp_id)
    if twofa.hash_code(code) != row["code_hash"]:
        return False
    db.delete_email_otp(emp_id)
    return True
```

Ajouter dans `backend/app/db.py` (si non fait en Task 1) :
```python
def stage_twofa_secret(emp_id: int, secret_encrypted: str) -> None:
    with get_conn() as conn:
        conn.execute("UPDATE employees SET twofa_secret_encrypted = ?, twofa_enabled = 0 WHERE id = ?",
                     (secret_encrypted, emp_id))
```

Enregistrer dans `backend/app/main.py` près des autres `include_router` :
```python
from .routers import ... twofa
app.include_router(twofa.router)
```

- [ ] **Step 4 : Lancer (passe) + suite complète**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_twofa_router.py -q && backend/.venv/bin/python -m pytest backend/tests/ -q`
Expected: PASS (router 3 tests + suite verte).

- [ ] **Step 5 : Commit**

```bash
git add backend/app/routers/twofa.py backend/app/db.py backend/app/main.py backend/tests/test_twofa_router.py
git commit -m "feat(2fa): router /2fa (setup TOTP/Email, verify, resend, appareil de confiance)"
```

---

### Task 6 : Branchement dans `auth.py login()` — TDD

**Files:**
- Modify: `backend/app/routers/auth.py` (login : appareil de confiance / 2FA après le gate Supabase)
- Test: `backend/tests/test_login_2fa.py`

**Interfaces:**
- Consumes: `db.is_device_trusted`, `twofa.hash_token`, `security.create_pre2fa_token`, cookie `sp_trust`.
- Produces : `POST /auth/login` renvoie soit `{access_token,...}` (appareil de confiance), soit `{"twofa_required": true, "pending_token": ...}` (2FA configurée), soit `{"twofa_setup_required": true, "pending_token": ...}` (pas encore configurée).

- [ ] **Step 1 : Écrire le test**

```python
# backend/tests/test_login_2fa.py
from fastapi.testclient import TestClient
import app.db as db, app.odoo as odoo, app.supabase_access as sa
from app.security import verify_pin  # noqa
import app.security as security
from app.main import app

client = TestClient(app)

def _prep(tmp_path, monkeypatch):
    monkeypatch.setattr(db.settings, "db_path", str(tmp_path / "t.db"))
    db.init_db()
    from app.security import hash_pin
    db.upsert_employee(1, "alex", "Alex", "tech", hash_pin("1234"))
    monkeypatch.setattr(sa, "access_decision", lambda *a, **k: {"allowed": True, "effective_tabs": [], "level": None})
    return db.get_employee_by_login("alex")

def test_login_requires_2fa_setup_first_time(tmp_path, monkeypatch):
    _prep(tmp_path, monkeypatch)
    r = client.post("/auth/login", json={"login": "alex", "pin": "1234"})
    assert r.status_code == 200
    assert r.json().get("twofa_setup_required") is True and "pending_token" in r.json()
    assert "access_token" not in r.json()

def test_login_trusted_device_skips_2fa(tmp_path, monkeypatch):
    emp = _prep(tmp_path, monkeypatch)
    db.set_twofa(emp["id"], "totp", "ENC")
    import app.twofa as twofa
    from datetime import datetime, timedelta, timezone
    token = "dev-token"
    db.add_trusted_device(emp["id"], twofa.hash_token(token),
                          (datetime.now(timezone.utc) + timedelta(days=30)).isoformat(), "")
    r = client.post("/auth/login", json={"login": "alex", "pin": "1234"}, cookies={"sp_trust": token})
    assert r.status_code == 200 and "access_token" in r.json()
```

- [ ] **Step 2 : Lancer (échoue)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_login_2fa.py -q`
Expected: FAIL.

- [ ] **Step 3 : Modifier `login()`**

Dans `backend/app/routers/auth.py`, importer en tête :
```python
from datetime import timezone
from fastapi import Request
from .. import db, supabase_access, twofa
from ..security import create_pre2fa_token
```
Remplacer la fin de `login` (le `return _issue_tokens(...)` final, après le gate Supabase) par :
```python
    # 2FA : si l'appareil n'est pas de confiance, exiger le 2e facteur.
    trust = request.cookies.get("sp_trust")
    from datetime import datetime
    trusted = bool(trust) and db.is_device_trusted(
        emp["id"], twofa.hash_token(trust), datetime.now(timezone.utc).isoformat())
    if not trusted:
        pending = create_pre2fa_token(emp["id"])
        if emp["twofa_enabled"]:
            return {"twofa_required": True, "pending_token": pending}
        return {"twofa_setup_required": True, "pending_token": pending}

    return _issue_tokens(emp["id"], emp["login"], emp["role"])
```
Et ajouter `request: Request` à la signature : `def login(body: LoginRequest, request: Request):`.

- [ ] **Step 4 : Lancer (passe) + suite**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_login_2fa.py backend/tests/ -q`
Expected: PASS, suite verte.

- [ ] **Step 5 : Commit**

```bash
git add backend/app/routers/auth.py backend/tests/test_login_2fa.py
git commit -m "feat(2fa): login exige le 2e facteur sauf appareil de confiance"
```

---

### Task 7 : Endpoint admin reset-2FA

**Files:**
- Modify: `backend/app/routers/admin.py`
- Test: `backend/tests/test_admin_reset_2fa.py`

**Interfaces:**
- Consumes: `require_admin` (existant dans admin.py), `db.reset_twofa`, `db.revoke_trusted_devices`.
- Produces: `POST /admin/employees/{id}/reset-2fa` → `{ ok: true }` (efface 2FA + appareils ; 404 si employé inconnu).

- [ ] **Step 1 : Écrire le test**

```python
# backend/tests/test_admin_reset_2fa.py
from fastapi.testclient import TestClient
import app.db as db
from app.main import app
from app.routers.admin import require_admin

client = TestClient(app)

def test_reset_2fa(tmp_path, monkeypatch):
    monkeypatch.setattr(db.settings, "db_path", str(tmp_path / "t.db"))
    db.init_db(); db.upsert_employee(1, "alex", "Alex", "tech", "h")
    emp = db.get_employee_by_login("alex")
    db.set_twofa(emp["id"], "totp", "ENC")
    app.dependency_overrides[require_admin] = lambda: {"id": 99, "role": "admin"}
    r = client.post(f"/admin/employees/{emp['id']}/reset-2fa")
    assert r.status_code == 200
    assert db.get_employee_by_id(emp["id"])["twofa_enabled"] == 0
    app.dependency_overrides.clear()
```

- [ ] **Step 2 : Lancer (échoue)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_admin_reset_2fa.py -q`
Expected: FAIL.

- [ ] **Step 3 : Ajouter l'endpoint dans `admin.py`**

```python
@router.post("/employees/{emp_id}/reset-2fa")
def reset_2fa(emp_id: int, _=Depends(require_admin)):
    if db.get_employee_by_id(emp_id) is None:
        raise HTTPException(404, "Employé introuvable")
    db.reset_twofa(emp_id)
    db.revoke_trusted_devices(emp_id)
    return {"ok": True}
```
(Importer `db`, `HTTPException`, `Depends` si nécessaire en tête du fichier.)

- [ ] **Step 4 : Lancer (passe)**

Run: `backend/.venv/bin/python -m pytest backend/tests/test_admin_reset_2fa.py -q`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add backend/app/routers/admin.py backend/tests/test_admin_reset_2fa.py
git commit -m "feat(2fa): reset 2FA par un admin (efface secret + appareils)"
```

---

### Task 8 : Frontend — écran 2FA + intégration login

**Files:**
- Create: `frontend/js/screens/twofa.js`
- Modify: `frontend/js/screens/login.js` (gérer `twofa_required`/`twofa_setup_required`)
- Modify: `frontend/js/api.js` (autoriser un appel avec le `pending_token` en en-tête)

Pas de test unitaire frontend (convention du repo) → validation par **smoke test manuel** + `node --check`.

- [ ] **Step 1 : Lire les patterns existants**

Lire `frontend/js/screens/login.js`, `frontend/js/api.js`, `frontend/js/store.js` et un écran type (`frontend/js/screens/moi.js`) pour réutiliser exactement le style (`api()`, `profile.set`, rendu d'écran, transition vers le dashboard).

- [ ] **Step 2 : Créer `frontend/js/screens/twofa.js`**

Écran à deux modes pilotés par la réponse du login :
- **setup** : si `can_email`, deux boutons « Authenticator » / « Email » ; sinon Authenticator seul.
  - Authenticator : `POST /2fa/setup/totp/start` (avec en-tête `pending_token`) → afficher le `otpauth_uri` en **QR** (générer via une petite lib QR vendue dans `frontend/js/` ou via l'API `otpauth_uri` rendue en image par un générateur léger) + le `secret` en repli + champ code → `POST /2fa/setup/totp/confirm {code, trust_device}`.
  - Email : `POST /2fa/setup/email/start` → champ code + bouton « renvoyer » (`/2fa/email/resend`) → `POST /2fa/setup/email/confirm {code, trust_device}`.
- **verify** : champ code (+ bouton renvoyer si méthode email) + case « faire confiance à cet appareil » → `POST /2fa/verify {code, trust_device}`.
- En cas de succès, la réponse contient `{access_token, refresh_token, expires_in}` → stocker comme le login normal (même chemin que `login.js`) et continuer vers le dashboard.

Le bouton « faire confiance à cet appareil » alimente `trust_device:true`. Les appels `/2fa/*` doivent partir avec `credentials:"include"` (pour poser le cookie d'appareil) et l'en-tête `Authorization: Bearer <pending_token>`.

- [ ] **Step 3 : Modifier `login.js`**

Après le `POST /auth/login`, si la réponse contient `twofa_required` ou `twofa_setup_required`, stocker `pending_token` et appeler `mountTwofa({mode, pendingToken, onDone})` au lieu d'aller au dashboard. Sinon, comportement actuel inchangé.

- [ ] **Step 4 : Modifier `api.js`**

Permettre un appel avec un token explicite (le `pending_token`) et `credentials:"include"` pour les routes `/2fa/*` (ex. option `opts.bearer` + `opts.credentials`).

- [ ] **Step 5 : Vérifs**

Run: `node --check frontend/js/screens/twofa.js && node --check frontend/js/screens/login.js && node --check frontend/js/api.js`
Expected: aucune erreur de syntaxe.
Puis **smoke manuel** (après déploiement) : login → écran setup → scanner le QR / recevoir le code → vérifier → « faire confiance » → relogin = pas de 2FA.

- [ ] **Step 6 : Commit**

```bash
git add frontend/js/screens/twofa.js frontend/js/screens/login.js frontend/js/api.js
git commit -m "feat(2fa): ecran 2FA (setup TOTP/Email + verify) + integration login"
```

---

## Self-Review

**Spec coverage :**
- Méthodes TOTP + Email au choix → Tasks 2,5,8. ✓
- Email masqué si pas de work_email (`can_email`) → Task 5 (`_can_email`), Task 8 (UI). ✓
- Code email 6 chiffres, exp 10 min, max 5 essais, anti-renvoi → Task 1 (otp), Task 5 (`_verify_email`, resend). ✓ (anti-renvoi 1/60s : à renforcer côté router si besoin — noté.)
- Secret TOTP chiffré (Fernet) → Task 2. ✓
- Appareil de confiance 30j, haché, cookie HttpOnly → Tasks 1,5,6. ✓
- Token pré-auth restreint à /2fa/* → Task 3 (deps), tous les endpoints /2fa gardés par `get_pre2fa_employee`. ✓
- Gate Supabase avant 2FA → Task 6 (insertion après le gate existant). ✓
- Reset admin → Task 7. ✓
- Login branche setup/required/trusted → Task 6. ✓
- Tests pytest → chaque task. ✓

**Placeholders :** code complet fourni ; le frontend (Task 8) suit la convention du repo (pas de tests unitaires JS) avec smoke manuel — non un placeholder mais la pratique établie. Le QR : prévoir une petite lib QR (à confirmer au moment du code ; repli = afficher le secret + le lien `otpauth`).

**Cohérence des types :** `set_twofa(method, enc)`, `stage_twofa_secret`, `is_device_trusted(emp_id, token_hash, now_iso)`, `hash_token`, `create_pre2fa_token`, `get_pre2fa_employee` utilisés de façon cohérente entre tasks 1→8. La forme de réponse `{access_token, refresh_token, expires_in}` correspond au `TokenResponse` existant (`auth._issue_tokens`).

## Notes de déploiement (hors plan, à l'exécution)
- `pip install pyotp cryptography` dans le venv VPS lors du déploiement.
- Cookie d'appareil de confiance : OK en prod (même origine `/api`). En dev cross-origin, prévoir `credentials:"include"` + CORS credentials.
- Migration `init_db` : s'exécute au démarrage du service ; les colonnes s'ajoutent automatiquement à la base existante.
